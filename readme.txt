# GIT

### It is a version control system - basically keeps the file track rather then making duplicate files it creates a change in the same file keeping the previous data in itself.

Clone → Bring Repo to your machine which is on the github platform.
Add → Track your file which you have it in your local machine
commit → save your files in git
push → upload git commands.
pull → download changes from remote repo.
